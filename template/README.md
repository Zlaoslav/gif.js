# Local template dependencies

This project was reduced to a single Python module: `gif_module.py`.

## Dependency policy
- No internet requests
- No external Python packages
- Only Python 3 standard library is required

## Quick start
```bash
python3 gif_module.py
```
This builds a local demo animation `demo.gif`.
